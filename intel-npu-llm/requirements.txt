# Core dependencies for Intel NPU LLM Server
# Install with: pip install -r requirements.txt
# Note: ipex-llm with NPU support must be installed first

fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
torch
transformers>=4.38.0
python-dotenv
psutil
